# Week 12 - Graphical User Interfaces (GUI) in Java

Full task instructions: see `WEEK 12 - Practical - GUI with Swing.docx` in the PRACTICALS folder.

**Files in this folder**

- `WelcomeGUI.java` - a window with a label and a button that changes the label's text when clicked.
- `RegistrationForm.java` - a small form with two text fields and a Submit button.

Run each one as a normal Java application (they each have their own `main` method) - a window should pop up.
